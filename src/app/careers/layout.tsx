import { CareersFrame } from "@/components/careers/careers-frame";

export default function CareersLayout({ children }: { children: React.ReactNode }) {
  return <CareersFrame>{children}</CareersFrame>;
}
