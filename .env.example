# Vercel project environment variables (Dashboard → Settings → Environment Variables)
# Never commit real values.

SPECTR_AUTH_USER=admin
SPECTR_AUTH_PASSWORD=change-me-to-a-long-random-password
SPECTR_AUTH_TOKEN_SECRET=optional-separate-signing-secret

# Where the AI Hedge Fund FastAPI app runs (your server or tunnel)
HEDGE_FUND_API_URL=http://127.0.0.1:8000
