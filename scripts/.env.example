# Spectr car scraper — Supabase credentials.
# The service-role key is required because the script writes to the database.
# Keep this file out of source control once filled in.

SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=replace-with-service-role-key
